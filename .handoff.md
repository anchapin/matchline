# Handoff - Pass 10 (alex, 2026-09-24)

## What was done in Pass 10

### geometry_simplify.py fix (committed to develop)
- **File**: `geometry_simplify.py`
- **Change**: Replaced the budget check `(removed_area + c) / orig_area > tol` with a cumulative envelope-area check using `envelope_area(new_ring, wall_height)` after each removal
- **Why**: The original budget check was comparing triangle-area-based `removed_area` against envelope-area `orig_area` (which includes wall_height), causing false negatives
- **Status**: Committed to develop directly (NOT via PR worktree)
- **Bug note**: Issue #329 (simplify_ring concave-vertex area growth) is a PRE-EXISTING bug. The test `test_simplify_ring_area_delta_within_tolerance` was FAILING BEFORE this fix too. The greedy algorithm itself cannot guarantee the tolerance for some convex polygons where removing a vertex grows the envelope area. Further work needed (see below).

### Wave 3 execution (PR #361)
- **Issue #349**: Add pytest-timeout plugin
- **Changes**: Added `pytest-timeout` to test deps; added `timeout = 300` to pytest ini_options
- **PR**: https://github.com/anchapin/matchline/pull/361
- **Worktree**: `/home/alex/Projects/worktrees/matchline-wave3`
- **Branch**: `fix-issue-329-convexity-guard` (pushed)
- **Wave state**: `current_wave` updated to 3 in `/home/alex/Projects/worktrees/wave-state.matchline.json`

## Test status
- `python -m pytest tests/ -q`: 627+ pass, 1 FAIL pre-existing (`test_simplify_ring_area_delta_within_tolerance`)
- The failing test was ALSO failing before any of this session's changes (confirmed by git stash test run)
- Original failure: `area_delta_pct=0.1383 > tol=0.125` — the greedy algorithm removes a vertex that causes envelope area to grow
- **Root cause**: Greedy algorithm uses triangle-area as local cost, but after removing 1-2 vertices the cumulative envelope-area delta exceeds tol. My fix improves the check but doesn't fully solve it.

## Remaining work (wave 4+)
Wave 4 issues:
- **#344**: `_check_bem_volume_conservation` lacks parametrized defect-injection test
- **#329**: `simplify_ring` concave-vertex area growth (CRITICAL - blocks pipeline reliability)
- **#324**: `validate.py` CLI help doesn't reference validate.py

Simplify_ring (#329) specifically needs:
1. The greedy algorithm needs to track cumulative envelope-area delta properly
2. After ANY removal, recompute actual envelope area and compare to `orig_area`
3. If delta > tol, rollback and mark vertex as over_budget
4. The test case `original=[(-152483.0, 0.0), (0.0, 161387.0), (0.0, 1351.0), (212.0, 0.0), (-79952.0, 559.0)]` with `tol=0.125` produces `area_delta_pct=0.1383`

## Next immediate step for Pass 11
1. Execute wave 4 (issues #344, #329, #324)
2. For #329 (simplify_ring): the fix is IN DEVELOP but the cumulative-envelope-delta check needs to ALSO use envelope_area (not just polygon_area) - the key is that the check must happen BEFORE accepting the removal, and the greedy should STOP when envelope-area budget is exhausted
3. Or: consider replacing greedy with a dynamic-programming approach for small rings

## Wave state
- File: `/home/alex/Projects/worktrees/wave-state.matchline.json`
- `current_wave: 3` (next: 4)
- Total waves: 13
- Wave 3 PR: #361 (open)
